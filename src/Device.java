

public class Device {
    static void main() {
        String testNeighbor = findNeighbors();
        System.out.println(testNeighbor);
    }
    public void DeviceClass (String ID, String ports, String IP){

    }

    public static String findNeighbors(){
        Parser printMate = new Parser();
        String GetNeighbors = Parser.getPath();
        return GetNeighbors;
    }

}